# Implementation Plan

## Current Change Plan：本週紀錄手動填寫

- 主頁右下角 `+` 新增 `填寫週曆` 入口。
- 新增 `填寫本週紀錄` modal，使用者可選本週任一天並輸入店家名稱。
- 週曆手動輸入的店名以 2 到 4 個字短名顯示，和抽籤自動寫入一致。
- 若輸入店名和既有餐廳完全相同，週曆紀錄同時保存餐廳 id；否則保存自訂店名。
- 保留既有抽籤與二選一自動寫入週曆功能，並兼容舊資料格式。
- 完成後執行 JS 語法檢查與 HTML selector 檢查。

## Current Change Plan：繁體翩翩體字體

- 全站字體改為優先使用繁體翩翩體。
- CSS font stack 使用 `HanziPen TC`、`翩翩體-繁`、`翩翩體` 作為優先字體。
- 若裝置沒有翩翩體，保留 `Microsoft JhengHei`、`Segoe UI`、Arial、sans-serif 作為 fallback，避免版面或可讀性受影響。
- 完成後檢查 CSS 字體設定是否已寫入。

## Current Change Plan：四頁 app 化與 PWA 安裝

- 將 `主頁 / 篩選 / 抽籤 / 設定` 統一成同一個 app viewport 規格：相同寬度、相同頁面最小高度、相同底部導覽安全距離。
- 修正 `篩選` 頁尺寸不一致問題，讓篩選頁和其他頁一樣填滿可用 app 高度，餐廳列表在頁面內自然排列。
- 新增 PWA `manifest.webmanifest`，讓 Android 與 iOS 可用瀏覽器加入主畫面。
- 新增 `service-worker.js`，快取 HTML/CSS/JS/seed/icon，支援離線開啟。
- 新增 app icon，供 manifest 與 iOS shortcut 使用。
- `index.html` 補上 PWA、iOS、theme color、apple mobile web app 相關 meta/link。
- `app.js` 註冊 service worker；若瀏覽器不支援則維持一般網頁模式。
- PWA 安裝需透過 `localhost` 或 HTTPS 開啟；直接用 `file://` 開啟仍可使用網頁，但不能註冊 service worker。
- 第一版仍維持純 HTML/CSS/JS，不加入 Capacitor、Cordova 或 App Store / Google Play 打包流程。
- 完成後執行 JS 語法檢查、service worker 語法檢查與 PWA 檔案存在檢查。

## Current Change Plan：週曆、左右淘汰賽卡片與抽籤動畫

- 二選一淘汰賽畫面改成明確的左右兩張餐廳卡片，卡片標示 `左邊`、`右邊`，使用者點卡片選擇。
- 主頁新增一週週曆，紀錄每日抽到或淘汰賽勝出的餐廳。
- 週曆中的餐廳名稱以 2 到 4 個字的短名稱顯示，讓格子保持簡潔。
- 抽籤按鈕按下後，結果區先快速閃爍餐廳卡片，再逐漸放慢，最後停在抽中的餐廳。
- 抽籤停止後跳出結果對話框，顯示抽到的餐廳，使用者點 `關閉` 後回到抽籤頁。
- 抽籤結果會寫入本週週曆的今天欄位。
- 二選一淘汰賽完成後，也會將勝出餐廳寫入本週週曆的今天欄位。
- 完成後執行 JS 語法檢查與 HTML selector 檢查。

## Current Change Plan：二選一輪數與底部操作位置

- 二選一淘汰賽開始時，隨機決定本場回合數為 10 到 15 回合。
- 二選一狀態文字顯示目前回合與本場總回合數。
- 左下角全域新增按鈕移到主頁內容的右下角，只在主頁出現。
- 新增選單跟著主頁右下角加號展開，不再固定在全站左下角。
- 底部導覽列改為填滿下方可用寬度，四個頁面按鈕平均分配寬度。
- 完成後執行 JS 語法檢查與 HTML selector 檢查。

## Current Change Plan：新增抽籤頁與二選一淘汰賽

- 底部導覽新增 `抽籤` 按鈕，位置放在 `篩選` 與 `設定` 中間。
- 將原本主頁的抽籤卡片移到新的 `抽籤` 頁面。
- 主頁改為簡潔入口，提供前往 `篩選` 與 `抽籤` 的操作按鈕，避免主頁搬空。
- 抽籤頁保留原本的隨機抽籤功能，仍依照目前篩選結果抽出餐廳。
- 抽籤頁新增 `二選一淘汰賽`，從目前篩選結果中隨機抓兩間餐廳讓使用者選擇。
- 二選一每回合保留使用者選中的餐廳，再隨機換一間挑戰者；完成本場隨機 10 到 15 回合後顯示勝出餐廳。
- 若目前篩選結果少於 2 間餐廳，二選一顯示不可開始的提示。
- 篩選條件變更或清除時，重置二選一淘汰賽狀態，避免候選池與畫面不一致。
- 完成後執行 JS 語法檢查與 HTML selector 檢查。

## Current Change Plan：新增底部篩選頁

- 底部導覽新增 `篩選` 按鈕，位置放在 `主頁` 與 `設定` 中間。
- 將原本主頁的 `篩選` 卡片移到新的 `篩選` 頁面。
- 將原本主頁的 `餐廳列表` 區塊移到新的 `篩選` 頁面。
- 主頁保留為快速入口；抽籤改放到 `抽籤` 頁。
- 餐廳列表卡片點進獨立說明頁後，`返回列表` 要回到 `篩選` 頁。
- 更新 JS 頁面切換邏輯，讓 `主頁 / 篩選 / 抽籤 / 設定` 四個底部導覽狀態正確同步。
- 完成後執行 JS 語法檢查與 HTML selector 檢查。

## Current Change Plan：篩選選項收合與標籤文字調整

- 將餐廳卡片與說明頁中的建議標籤顯示改為直接顯示標籤名稱，不再加上 `AI` 前綴。
- 將餐廳說明頁的 `AI 建議` 欄位改為較中性的 `建議說明`。
- 將主頁篩選卡片中的 `只看待吃` 與 `顯示避雷` 從主要欄位移出。
- 在 `目標地點` 旁新增 `篩選選項` 按鈕，點擊後顯示 `只看待吃` 與 `顯示避雷`。
- `篩選選項` 按鈕顯示目前啟用的附加篩選數量，讓篩選卡片保持簡潔但仍可快速辨識狀態。
- 完成後執行 JS 語法檢查，確認篩選狀態、餐廳列表與說明頁仍可正常渲染。

## Current Change Plan：新莊高中附近初始餐廳資料

- 將 `seed-data.js` 的「新莊高中附近」初始餐廳從 50 筆改為 Google Maps 清單 `https://maps.app.goo.gl/tQw8kB4tXhChFiSWA` 內的 10 筆地點。
- 餐廳順序依 Google Maps 清單順序寫入。
- 每筆餐廳加入可點擊的 Google Maps 搜尋連結。
- App 只有在第一次開啟且尚未有任何餐廳資料時自動載入，不覆蓋使用者既有資料。
- 設定頁提供「載入新莊高中附近 10 筆」按鈕，讓已有資料的使用者也能手動匯入，重複餐廳略過。
- 建立 `food_maps/xinzhuang-high-school/` 資料夾，保存此批初始資料的地圖設定與來源備註。
- 完成後執行 JS 語法檢查與初始資料筆數檢查。

## Current Change Plan：類別與標籤多選 UI

- 將食物類別與自訂標籤改為參考圖樣式的 chip 多選元件。
- 收合狀態只顯示已選 chip 與輸入框，不把所有選項鋪在畫面上。
- 點擊輸入區後展開選單，選單內顯示可捲動選項列表。
- 已選項目以彩色 chip 顯示，chip 旁提供移除按鈕。
- 輸入不存在的名稱時可建立新的食物類別或自訂標籤。
- 餐廳新增表單與主頁篩選區都使用同一套 chip 多選元件。
- 更新 JS 讀取多選值、渲染選項、建立新選項與同步 state 的邏輯。
- 完成後確認新增餐廳、篩選、匯入自動建立類別/標籤仍正常。

## Current Change Plan：餐廳獨立說明頁與修改資料

- 餐廳列表每一張卡片都可以點擊進入獨立說明頁。
- 餐廳卡片內的功能按鈕與 Google Maps 連結維持原本行為，不觸發進入詳情頁。
- 獨立說明頁顯示餐廳完整資訊：名稱、目標地點、地址、Google Maps、類別、標籤、AI 建議、距離、備註、私人筆記、資料確認狀態。
- 獨立說明頁提供「修改資料」按鈕。
- 修改資料按鈕開啟餐廳表單並載入目前資料，送出後更新原餐廳，不新增重複餐廳。
- 詳情頁提供返回列表按鈕。

## 1. 專案方向

建立一個純前端餐廳食物選擇 Web App。

新版第一版目標是「能記錄、能匯入、能篩選、能收藏、能避雷、能抽籤」。

技術選擇：

- `index.html`：頁面結構。
- `styles.css`：版面與視覺樣式。
- `app.js`：資料狀態、互動邏輯、localStorage。

限制：

- 不使用前端框架。
- 不使用 npm。
- 不使用後端。
- 不串接 Google Maps API。
- 不自動爬取 Google Maps。
- 使用者可直接開啟 `index.html`。

## 2. 第一版功能優先順序

第一版優先完成：

1. AI 找尋地點文件與資料夾格式。
2. 底部主頁 / 設定導覽。
3. 左下角加號新增選單。
4. 目標地點建立。
5. 餐廳、類別、自訂標籤新增。
6. JSON 匯入清單，放在設定頁。
7. AI 建議標籤標示。
8. 願望清單與黑名單標記。
9. 根據目標地點、類別、標籤、願望清單狀態篩選。
10. 黑名單預設排除。
11. 上一次篩選條件自動保存。
12. 根據目前篩選結果抽籤。
13. 餐廳私人筆記。
14. localStorage 保存。

第一版先不做：

- CSV 匯入。
- 多組常用篩選條件。
- 多筆造訪紀錄。
- 完整編輯餐廳。
- 類別與標籤刪除。
- 自動讀取資料夾。
- 實體地圖視覺化。

## 3. Phase 1：建立基本檔案

預計建立：

- `index.html`
- `styles.css`
- `app.js`

工作內容：

- 建立 HTML 基本結構。
- 引入 CSS 與 JS。
- 定義主要畫面區塊。

完成條件：

- 直接打開 `index.html` 不報錯。
- 頁面可看到主要區塊。

## 3.1 Phase 1A：底部主頁 / 設定與新增入口

調整畫面結構：

- 底部建立「主頁」與「設定」按鈕。
- 主頁只顯示快速入口。
- 篩選頁顯示篩選與餐廳列表。
- 抽籤頁顯示隨機抽籤與二選一淘汰賽。
- 設定顯示匯入清單與相關說明。
- 新增資料表單不直接顯示在主畫面。
- 左下角固定加號按鈕開啟新增選單。
- 新增目標地點、餐廳、類別、標籤改由彈出視窗開啟。

完成條件：

- 點擊主頁 / 設定可以切換內容。
- 點擊左下角加號可以看到新增選項。
- 新增表單不會在主頁直接佔版面。

## 4. Phase 2：AI 找尋地點文件與資料夾規格

新增文件：

- `ai place research guide.md`

文件內容需定義：

- 如何使用線下 AI 整理 Google 地點資料。
- 建議標籤分類。
- JSON 匯入格式。
- CSV 匯入欄位。
- 目標地點附近美食地圖資料夾結構。
- 人工確認清單。

建議資料夾結構：

```text
food_maps/
  target-place-name/
    map profile.md
    import list.json
    import list.csv
    ai tag suggestions.md
    source notes.md
```

完成條件：

- 文件清楚描述 AI 只協助整理資料，不自動爬取 Google Maps。
- 文件提供可複用的 AI prompt。
- 文件提供匯入 JSON 範例。

## 5. Phase 3：建立資料模型與狀態

在 `app.js` 中建立前端狀態。

需要的 state：

```js
const state = {
  targetAreas: [],
  importBatches: [],
  categories: [],
  tags: [],
  restaurants: [],
  savedFilters: [],
  selectedCategoryIds: [],
  selectedTagIds: [],
  wishlistOnly: false,
  showBlacklisted: false,
  selectedTargetAreaId: null,
  lastLotteryRestaurantId: null
};
```

需要的 helper：

- `createId(prefix)`
- `loadState()`
- `saveState()`
- `normalizeState(rawState)`
- `getCategoryName(id)`
- `getTagName(id)`
- `getTargetAreaName(id)`
- `getFilteredRestaurants()`
- `pickRandomRestaurant(restaurants)`
- `validateImportPayload(payload)`
- `previewImport(payload)`
- `applyImport(preview)`

完成條件：

- App 啟動時可從 `localStorage` 載入資料。
- 資料異動後可保存到 `localStorage`。
- localStorage 資料壞掉時可回到空狀態。

## 6. Phase 4：目標地點功能

實作功能：

- 建立目標地點資料模型。
- 餐廳可隸屬於某個目標地點。
- 篩選區可切換目標地點。

目標地點資料：

- 目標名稱。
- 中心點描述。
- 搜尋範圍。
- 使用情境。

完成條件：

- 可以保存目標地點。
- 餐廳可記錄 `targetAreaId`。
- 切換目標地點後，餐廳列表只顯示該目標附近資料。
- 未選擇目標地點時，可顯示全部餐廳。

## 7. Phase 5：食物類別功能

實作功能：

- 新增食物類別。
- 類別名稱驗證。
- 防止重複類別。
- 渲染類別 checkbox。

完成條件：

- 可以新增多個類別。
- 類別會保存。
- 重整頁面後類別仍存在。

## 8. Phase 6：自訂標籤功能

實作功能：

- 新增自訂標籤。
- 標籤名稱驗證。
- 防止重複標籤。
- 渲染標籤 checkbox。

完成條件：

- 可以新增多個自訂標籤。
- 標籤會保存。
- 餐廳可以套用多個標籤。

## 9. Phase 7：餐廳功能

實作功能：

- 新增餐廳。
- 選擇目標地點。
- 選擇餐廳對應的食物類別。
- 選擇餐廳對應的自訂標籤。
- 新增地址與 Google Maps 連結，可選。
- 新增公開備註。
- 新增私人筆記。
- 記錄 AI 建議標籤與資料確認狀態。
- 刪除餐廳。

完成條件：

- 可以新增餐廳。
- 餐廳可有目標地點。
- 餐廳可有多個類別。
- 餐廳可有多個標籤。
- 餐廳可保存私人筆記。
- 餐廳可保存地址與 Google Maps 連結。
- 餐廳可保存 AI 建議標籤與確認狀態。
- 可以刪除餐廳。
- 資料可保存。

## 10. Phase 8：JSON 匯入清單功能

第一版先支援 JSON 匯入。

實作功能：

- 選擇或貼上 JSON 匯入資料。
- 驗證匯入格式。
- 預覽匯入結果。
- 顯示新增筆數、重複筆數、錯誤筆數。
- 確認後寫入 state。

匯入規則：

- 餐廳名稱不可空白。
- 餐廳名稱與地址都相同時視為可能重複。
- 重複餐廳預設不新增。
- `categoryNames` 匯入後自動建立不存在的類別。
- `tagNames` 匯入後自動建立不存在的標籤。
- `aiSuggestedTagNames` 要保存為 AI 建議，不直接視為使用者已確認。
- `dataVerified` 預設為 false。

完成條件：

- 可以匯入 JSON 清單。
- 匯入前有預覽。
- 匯入後餐廳、類別、標籤會更新。
- 匯入後資料會保存到 localStorage。
- 錯誤格式不會破壞既有資料。

## 11. Phase 9：願望清單功能

實作功能：

- 餐廳卡片提供「下次吃」切換。
- 切換餐廳的 `isWishlisted`。
- 篩選區提供「只看願望清單」切換。

完成條件：

- 餐廳可以加入願望清單。
- 餐廳可以移出願望清單。
- 願望清單狀態會保存。
- 開啟願望清單篩選時，只顯示已收藏餐廳。

## 12. Phase 10：黑名單功能

實作功能：

- 餐廳卡片提供「避雷」切換。
- 切換餐廳的 `isBlacklisted`。
- 搜尋結果預設排除黑名單餐廳。
- 篩選區提供「顯示黑名單」切換。

完成條件：

- 餐廳可以加入黑名單。
- 餐廳可以移出黑名單。
- 黑名單狀態會保存。
- 黑名單餐廳預設不顯示。
- 開啟顯示黑名單後可以看見黑名單餐廳。

## 13. Phase 11：篩選功能

實作功能：

- 切換目標地點。
- 勾選一個或多個食物類別。
- 勾選一個或多個自訂標籤。
- 切換願望清單篩選。
- 切換顯示黑名單。
- 即時更新餐廳列表。
- 清除所有篩選。
- 顯示結果數量。

篩選規則：

- 未勾選任何條件：顯示所有啟用且未黑名單的餐廳。
- 選擇目標地點：只顯示該目標地點底下的餐廳。
- 有勾選類別：餐廳需符合任一勾選類別。
- 有勾選標籤：餐廳需符合任一勾選標籤。
- 同時勾選類別與標籤：餐廳需同時符合類別條件與標籤條件。
- 願望清單篩選開啟：餐廳需已加入願望清單。
- 顯示黑名單未開啟：黑名單餐廳直接排除。

完成條件：

- 目標地點篩選正常。
- 勾選條件後列表正確更新。
- 取消勾選後列表正確恢復。
- 願望清單篩選正常。
- 黑名單排除正常。
- 沒有符合結果時有清楚提示。

## 14. Phase 12：常用篩選條件紀錄

第一版先實作「自動記住上一次篩選條件」。

需要保存：

- `selectedTargetAreaId`
- `selectedCategoryIds`
- `selectedTagIds`
- `wishlistOnly`
- `showBlacklisted`

完成條件：

- 重新整理後，checkbox 狀態與切換狀態保持一致。
- 重新整理後，目標地點選擇保持一致。
- 篩選結果依照還原後的條件顯示。

第二版再加入多組常用條件，例如「午餐」、「晚餐」、「健康餐」。

## 15. Phase 13：抽籤功能

實作功能：

- 從目前篩選後的餐廳中隨機挑一間。
- 顯示抽籤結果。
- 顯示餐廳名稱、目標地點、類別、標籤、備註與私人筆記摘要。
- 沒有結果時顯示提示。

抽籤規則：

- 只從目前篩選結果抽。
- 不抽停用餐廳。
- 不抽黑名單餐廳。
- 若選擇目標地點，只從該目標地點抽。
- 若願望清單篩選開啟，只從願望清單抽。

完成條件：

- 每次點擊可得到目前結果中的隨機餐廳。
- 不會抽到已被篩選排除的餐廳。
- 不會抽到黑名單餐廳。
- 不會抽到其他目標地點的餐廳。
- 沒有可抽項目時有清楚提示。

## 16. Phase 14：私人筆記與已造訪基礎

第一版先實作餐廳層級私人筆記。

實作功能：

- 新增餐廳時可填私人筆記。
- 餐廳卡片顯示私人筆記摘要。
- 私人筆記保存於 localStorage。

第二版可加入：

- 標記已造訪。
- 多筆造訪日期。
- 每次造訪的獨立筆記。

完成條件：

- 私人筆記可新增。
- 私人筆記可保存。
- 重新整理後私人筆記仍存在。

## 17. Phase 15：版面與使用體驗

實作重點：

- 桌面版採左右或上下分區，方便新增與查看結果。
- 手機版採單欄排列。
- checkbox、按鈕、輸入框大小要好點擊。
- 餐廳結果要容易掃描。
- 目標地點切換要容易理解。
- 匯入預覽要避免誤操作。
- AI 建議標籤與已確認標籤要容易區分。
- 願望清單與黑名單狀態要明顯。
- 抽籤結果要突出但不干擾列表。

完成條件：

- 手機與桌面都可正常操作。
- 文字不重疊。
- 表單與結果區不擁擠。
- 餐廳卡片資訊清楚。

## 18. Phase 16：測試與驗收

手動測試清單：

- 新增目標地點可保存。
- 新增空白類別會被阻擋。
- 新增重複類別會被阻擋。
- 新增空白標籤會被阻擋。
- 新增重複標籤會被阻擋。
- 新增餐廳後列表立即更新。
- 新增餐廳可記錄目標地點。
- 餐廳可套用類別。
- 餐廳可套用標籤。
- JSON 匯入格式正確時可預覽。
- JSON 匯入格式錯誤時不寫入資料。
- 匯入後自動建立新類別。
- 匯入後自動建立新標籤。
- 匯入後 AI 建議標籤狀態保留。
- 重複餐廳不會重複新增。
- 目標地點篩選正確。
- 勾選類別後只顯示符合餐廳。
- 勾選標籤後只顯示符合餐廳。
- 同時勾選類別與標籤時篩選正確。
- 願望清單切換會保存。
- 只看願望清單篩選正確。
- 黑名單切換會保存。
- 黑名單餐廳預設不顯示。
- 顯示黑名單切換正確。
- 抽籤只從目前結果挑選。
- 抽籤不會抽到黑名單餐廳。
- 抽籤不會抽到其他目標地點的餐廳。
- 私人筆記可保存。
- 清除篩選後顯示所有非黑名單餐廳。
- 重新整理後資料仍存在。
- 重新整理後上一次篩選條件仍存在。
- 手機寬度下版面正常。

## 19. 建議第一版完成檔案

```text
food_webapp/
  index.html
  styles.css
  app.js
  ai place research guide.md
  food_maps/
    target-place-name/
      map profile.md
      import list.json
      import list.csv
      ai tag suggestions.md
      source notes.md
  developer document.md
  implementation plan.md
```

## 20. 實作注意事項

- 不要把資料寫死在 HTML 中，應由 JS render。
- 每次 state 變更後要重新 render 並保存。
- DOM selector 命名要清楚。
- 表單 submit 要使用 `event.preventDefault()`。
- localStorage 讀取失敗時要有 fallback 空資料。
- 篩選邏輯要集中在 `getFilteredRestaurants()`，避免分散到多個 event handler。
- 黑名單排除邏輯要同時套用在列表與抽籤。
- 匯入功能要先預覽再寫入。
- 匯入失敗不可清空原本資料。
- AI 建議標籤要標示為未確認，避免和使用者確認過的標籤混在一起。
- Google Maps 資料只作為使用者手動整理來源，不在 App 內自動抓取。
- UI 文字保持簡單直覺。

## 21. 預估開發步驟

1. 建立三個前端檔案。
2. 建立 AI 找尋地點文件。
3. 寫好 HTML 結構。
4. 完成 CSS 基本版面。
5. 完成 state 與 localStorage。
6. 完成目標地點資料模型。
7. 完成類別新增與渲染。
8. 完成標籤新增與渲染。
9. 完成餐廳新增與渲染。
10. 完成 JSON 匯入預覽。
11. 完成 JSON 匯入寫入。
12. 完成願望清單切換。
13. 完成黑名單切換。
14. 完成篩選。
15. 完成上一次篩選條件保存。
16. 完成抽籤。
17. 完成私人筆記。
18. 做手機版調整。
19. 完成手動測試。
