# 餐廳食物選擇 Web App 開發者文件

## Current Requirement Update：2026-06-04

- 二選一淘汰賽使用左右兩張餐廳卡片呈現，點擊左卡或右卡代表本回合選擇。
- 主頁新增週曆，紀錄每天抽到或淘汰賽勝出的店家，格內只顯示店名短字 2 到 4 個字。
- 隨機抽籤加入挑選動畫：結果區快速閃店家卡片，逐漸變慢後停止，並跳出結果對話框讓使用者關閉。
- 二選一淘汰賽每次開始時隨機產生 10 到 15 回合，使用者完成該場回合後才顯示勝出餐廳。
- 新增資料加號按鈕移到主頁內容右下角，並只在主頁作為新增入口出現。
- 底部導覽列填滿下方寬度，`主頁 / 篩選 / 抽籤 / 設定` 四個按鈕平均分配。
- 底部導覽新增 `抽籤` 頁，放在 `篩選` 與 `設定` 之間。
- 原本主頁的抽籤功能移到 `抽籤` 頁；主頁保留為簡潔入口。
- 抽籤頁新增 `二選一淘汰賽`：系統從目前篩選結果隨機挑兩間餐廳，使用者直覺選一間，完成本場隨機 10 到 15 回合後產生勝出餐廳。
- 底部導覽新增 `篩選` 頁，放在 `主頁` 與 `設定` 之間。
- 原本主頁的篩選卡片與餐廳列表移到 `篩選` 頁；主頁保留為快速入口。
- 餐廳獨立說明頁的返回動作改為回到 `篩選` 頁的餐廳列表。
- 建議標籤在畫面上直接顯示標籤名稱，不再額外加上 `AI` 字樣；說明頁的建議備註欄位改稱 `建議說明`。
- 主頁篩選卡片保持簡潔：`只看待吃` 與 `顯示避雷` 收合到 `目標地點` 旁的 `篩選選項` 按鈕中。
- 食物類別與自訂標籤改為參考圖樣式的 chip 多選元件：收合時只顯示已選 chip，點開後顯示可捲動選單，並可輸入建立新選項。
- 新莊高中附近初始資料改為使用者提供的 Google Maps 清單 `https://maps.app.goo.gl/tQw8kB4tXhChFiSWA` 內 10 個地點。
- 每筆初始餐廳保留可點擊的 Google Maps 搜尋連結。
- 若瀏覽器 localStorage 內仍有舊版 50 筆初始 seed，啟動時會移除舊 seed 並改載入新版 10 筆。
- 餐廳列表卡片可點擊進入獨立說明頁，說明頁內提供修改資料按鈕。

## 1. 專案目的

建立一個純前端 Web App，讓使用者可以記錄餐廳、食物類別、個人標籤、用餐經驗與目標地點附近的美食清單，並透過勾選條件快速找出「現在想吃什麼」。

此版本只使用最簡單的：

- HTML
- CSS
- JavaScript

不使用後端、不使用資料庫、不使用前端框架、不串接 Google Maps API、不自動爬取 Google Maps。

所有資料先存在瀏覽器 `localStorage`，使用者可以直接打開 `index.html` 使用。

## 2. 核心使用情境

使用者每天不知道要吃什麼時，可以：

1. 新增常吃或想吃的餐廳。
2. 替餐廳標記食物類別，例如：飯、麵、火鍋、速食、飲料、甜點。
3. 加上自訂標籤，例如：有冷氣、200 以內、適合聚餐、外帶快。
4. 匯入由線下 AI 整理過的 Google 地點清單。
5. 建立某個目標地點周圍的美食地圖資料。
6. 勾選今天想吃的條件。
7. App 根據條件列出符合的餐廳。
8. 將想去但還沒去的店家加入願望清單。
9. 將不想再看到的店家加入黑名單。
10. 從目前篩選結果中進行抽籤，隨機決定要吃哪一間。
11. 造訪後記錄日期與私人筆記，方便下次回想。

## 3. 目標使用者

- 想整理常吃餐廳的人。
- 選午餐、晚餐時容易猶豫的人。
- 想記錄待吃清單、踩雷店家、私人口味筆記的人。
- 想把「下次吃什麼」變成可篩選、可抽籤流程的人。
- 想針對公司、家、捷運站或旅遊景點建立附近美食資料的人。

## 4. 功能範圍

### 4.1 餐廳管理

使用者可以新增餐廳資料。

建議欄位：

- 餐廳名稱
- 地址，可選
- Google Maps 連結，可選
- 目標地點
- 食物類別
- 自訂標籤
- AI 建議標籤
- 備註
- 私人筆記
- 是否啟用
- 是否加入願望清單
- 是否加入黑名單
- 資料是否已人工確認

第一版建議至少支援新增、刪除與狀態切換。完整編輯功能可以先列為第二版。

### 4.2 食物類別管理

使用者可以新增食物類別。

範例：

- 飯
- 麵
- 火鍋
- 便當
- 速食
- 飲料
- 甜點
- 健康餐
- 日式
- 韓式
- 美式

食物類別會用 checkbox 呈現，使用者可以勾選多個條件。

### 4.3 自訂標籤

除了食物類別，使用者可以建立自己的標籤。

範例：

- 有冷氣
- 200 以內
- 適合聚餐
- 外帶快
- 不用排隊
- 宵夜
- 離公司近
- 高蛋白

食物類別偏向「吃什麼」，自訂標籤偏向「用餐條件或個人偏好」。

### 4.4 AI 建議標籤

線下 AI 可以根據使用者整理的 Google 地點資料、店名、評論摘要、地址與人工筆記，建議可能適合的標籤。

AI 建議標籤需要和使用者確認過的標籤分開記錄。

建議標籤分類：

- 食物類型：台式、日式、韓式、義式、美式、泰式、港式、越式、早餐、咖啡、甜點、飲料、火鍋、燒肉、麵食、便當。
- 用餐情境：午餐、晚餐、宵夜、聚餐、一人友善、約會、家庭、外帶、內用、可久坐。
- 條件限制：200 以內、300 以內、有冷氣、可訂位、不用排隊、交通方便、離目標近。
- 口味偏好：清爽、重口味、高蛋白、低負擔、素食友善、辣、湯品、份量大。
- 風險標籤：可能排隊、座位少、價格偏高、評價兩極、營業時間不穩定。
- 資料狀態：AI 建議、使用者已確認、需要複查、資料可能過期。

### 4.5 願望清單 / 待吃清單

使用者可以把餐廳標記為「下次吃」。

用途：

- 篩選出結果後，不一定馬上吃，可以先收藏。
- 之後可以快速查看所有待吃店家。
- 抽籤時可以選擇只從願望清單抽。

互動建議：

- 餐廳卡片上提供「下次吃」切換按鈕。
- 已加入願望清單的餐廳用明顯標記顯示。

### 4.6 黑名單 / 避雷針

使用者可以把特定餐廳標記為「不再顯示」。

規則：

- 黑名單餐廳預設不出現在搜尋結果。
- 黑名單餐廳不參與抽籤。
- 可以提供「顯示黑名單」切換，方便使用者恢復店家。

這是核心功能，因為它可以避免使用者反覆看到不想吃或踩雷過的店家。

### 4.7 目標周圍美食地圖

使用者可以針對某一個目標地點建立附近美食資料。

目標地點範例：

- 公司附近
- 家附近
- 捷運站附近
- 某間學校附近
- 某個旅遊景點附近

每個目標地點可以記錄：

- 目標名稱
- 中心點描述
- 搜尋半徑或步行時間
- 使用情境
- 整理日期
- 資料確認狀態

建議以資料夾形式保存整理資料：

```text
food_maps/
  taipei-main-station/
    map profile.md
    import list.json
    import list.csv
    ai tag suggestions.md
    source notes.md
```

第一版 Web App 不一定需要直接讀取資料夾，但文件與資料格式要先定義好，方便後續新增資料。

### 4.8 匯入清單

使用者可以匯入一份由自己或線下 AI 整理過的餐廳清單。

匯入來源可以是：

- 使用者從 Google Maps 手動整理的店家資料。
- 使用者貼給線下 AI 後，由 AI 整理出的 JSON。
- 使用者貼給線下 AI 後，由 AI 整理出的 CSV。
- 特定目標地點附近的美食地圖資料夾。

第一版建議支援 JSON 匯入，第二版再支援 CSV 匯入。

重要規則：

- App 不主動爬取 Google Maps。
- App 不串接 Google Maps API。
- Google 地點資訊由使用者或線下 AI 先整理成匯入檔。
- 匯入前先預覽，不直接寫入資料。
- 匯入後仍允許使用者手動修正標籤與筆記。

### 4.9 條件勾選與搜尋

使用者可以透過 checkbox 勾選食物類別與自訂標籤。

搜尋邏輯：

- 沒有勾選任何條件時，顯示所有啟用且未進黑名單的餐廳。
- 勾選食物類別時，顯示符合任一類別的餐廳。
- 勾選自訂標籤時，顯示符合任一標籤的餐廳。
- 若同時勾選類別與標籤，餐廳需同時符合「類別條件」與「標籤條件」。
- 選擇目標地點時，只顯示該目標地點底下的餐廳。
- 願望清單可以作為額外篩選條件。
- 黑名單預設永遠排除。

### 4.10 常用篩選條件紀錄

App 需要減少重複操作。

支援兩種方式：

- 自動記住上一次勾選狀態。
- 讓使用者儲存幾組常用條件組合。

第一版可先完成「自動記住上一次勾選狀態」，儲存多組條件可列為進階功能。

### 4.11 已造訪與私密筆記

使用者可以記錄曾經去過餐廳的日期與私人備註。

第一版可先提供一個餐廳層級的私人筆記欄位。多筆造訪紀錄可列為第二版。

### 4.12 抽籤功能

使用者可以根據目前篩選條件進行抽籤。

規則：

- 抽籤來源為目前篩選後的餐廳清單。
- 黑名單餐廳不可被抽到。
- 停用餐廳不可被抽到。
- 若選擇目標地點，只能從該目標地點抽。
- 若啟用願望清單篩選，只能從願望清單中抽。
- 若目前沒有符合結果，顯示空狀態提示。

### 4.13 本機資料保存

因為不使用後端，資料先存在瀏覽器 `localStorage`。

保存內容：

- 目標地點列表
- 匯入批次紀錄
- 餐廳列表
- 食物類別列表
- 自訂標籤列表
- AI 建議標籤
- 願望清單狀態
- 黑名單狀態
- 私人筆記
- 已造訪紀錄
- 使用者最後勾選的搜尋條件
- 常用條件組合，可選

## 5. 非功能需求

### 5.1 技術限制

- 不使用 React、Vue、Svelte 等框架。
- 不使用 npm。
- 不需要 build step。
- 不使用後端 API。
- 不串接地圖服務。
- 不自動爬取 Google Maps。
- 使用者可以直接打開 `index.html` 使用。

### 5.2 相容性

目標支援現代瀏覽器：

- Chrome
- Edge
- Firefox
- Safari

### 5.3 RWD

版面需支援：

- 桌面寬螢幕
- 平板
- 手機

手機版應以單欄操作為主，checkbox、輸入框與按鈕要容易點擊。

## 6. 建議檔案結構

```text
food_webapp/
  index.html
  styles.css
  app.js
  ai place research guide.md
  food_maps/
    target-place-name/
      map profile.md
      import list.json
      import list.csv
      ai tag suggestions.md
      source notes.md
  developer document.md
  implementation plan.md
```

## 7. 建議資料結構

### 7.1 Category

```js
{
  id: "cat_001",
  name: "麵"
}
```

### 7.2 Custom Tag

```js
{
  id: "tag_001",
  name: "有冷氣"
}
```

### 7.3 Target Area

```js
{
  id: "area_001",
  name: "台北車站附近",
  centerName: "台北車站",
  radiusText: "步行 10-15 分鐘",
  purpose: "上班日午餐與晚餐",
  createdAt: "2026-06-03T12:00:00.000Z"
}
```

### 7.4 Visit Record

```js
{
  id: "visit_001",
  visitedAt: "2026-06-03",
  note: "招牌布丁必點，下午兩點前去才不會賣完。"
}
```

### 7.5 Restaurant

```js
{
  id: "res_001",
  targetAreaId: "area_001",
  name: "巷口牛肉麵",
  address: "台北市中正區範例路 1 號",
  googleMapsUrl: "https://maps.google.com/?q=example",
  categoryIds: ["cat_001"],
  tagIds: ["tag_001"],
  aiSuggestedTagIds: ["tag_001"],
  aiSuggestionNote: "AI 根據店名與評論摘要判斷為麵食、午餐、200 以內。",
  dataVerified: false,
  source: "manual-google-maps-offline-ai",
  priceText: "約 100-200",
  rating: 4.2,
  reviewCount: 328,
  note: "湯頭濃，晚餐常吃",
  privateNote: "下次可以點小碗，份量剛好。",
  isWishlisted: false,
  isBlacklisted: false,
  enabled: true,
  visitRecords: [],
  createdAt: "2026-06-03T12:00:00.000Z"
}
```

### 7.6 Import Batch

```js
{
  id: "import_001",
  targetAreaId: "area_001",
  sourceType: "offline-ai-google整理",
  importedAt: "2026-06-03T12:00:00.000Z",
  restaurantCount: 24,
  note: "由使用者貼上 Google Maps 搜尋結果後，線下 AI 整理。"
}
```

### 7.7 Saved Filter

```js
{
  id: "filter_001",
  name: "午餐",
  categoryIds: ["cat_001"],
  tagIds: ["tag_001"],
  wishlistOnly: false,
  targetAreaId: "area_001"
}
```

### 7.8 App State

```js
{
  targetAreas: [],
  importBatches: [],
  categories: [],
  tags: [],
  restaurants: [],
  savedFilters: [],
  selectedCategoryIds: [],
  selectedTagIds: [],
  wishlistOnly: false,
  showBlacklisted: false,
  selectedTargetAreaId: null,
  lastLotteryRestaurantId: null
}
```

## 8. 畫面區塊規劃

### 8.1 上方標題區

- 顯示 App 名稱。
- 顯示目前共有幾間餐廳、幾間待吃、幾間黑名單。
- 顯示目前選擇的目標地點。

### 8.2 底部主頁 / 設定導覽

畫面底部提供兩個主要入口：

- 主頁：放快速入口。
- 篩選：放篩選卡片與餐廳列表。
- 抽籤：放隨機抽籤與二選一淘汰賽。
- 設定：放匯入清單、AI 匯入說明與資料管理入口。

切換頁面時不重新整理頁面，只切換目前顯示區塊。

### 8.3 左下角新增按鈕

新增資料不直接常駐顯示在主畫面，改成左下角加號按鈕。

點擊加號後顯示新增選單：

- 新增目標地點
- 新增餐廳
- 新增食物類別
- 新增自訂標籤

每個新增項目以彈出視窗或獨立面板顯示，送出或取消後關閉。

### 8.4 新增資料區

包含：

- 新增目標地點表單。
- 新增食物類別表單。
- 新增自訂標籤表單。
- 新增餐廳表單。

### 8.5 條件篩選區

包含：

- 目標地點選擇。
- 食物類別 checkbox 清單。
- 自訂標籤 checkbox 清單。
- 願望清單篩選切換。
- 顯示黑名單切換。
- 清除篩選按鈕。
- 顯示目前符合結果數量。

### 8.6 匯入清單區

包含：

- 選擇或貼上 JSON 匯入資料。
- 顯示匯入預覽。
- 顯示新增幾筆、重複幾筆、錯誤幾筆。
- 匯入確認按鈕。

第一版可先只支援 JSON 匯入。

此區塊放在「設定」頁，不放在主頁。

### 8.7 結果列表區

每張餐廳資料包含：

- 餐廳名稱
- 目標地點
- 地址與 Google Maps 連結，可選
- 食物類別標籤
- 自訂標籤
- AI 建議標籤
- 資料確認狀態
- 備註
- 私人筆記摘要
- 願望清單狀態
- 黑名單狀態
- 「下次吃」切換按鈕
- 「避雷」切換按鈕
- 刪除按鈕

### 8.8 抽籤區

包含：

- 抽籤按鈕。
- 抽籤結果顯示。
- 無結果提示。

抽籤結果必須來自目前篩選結果。

## 9. 互動規則

### 9.1 新增目標地點

- 目標名稱不可空白。
- 目標名稱不可重複。
- 新增後可在篩選區選擇。

### 9.2 新增食物類別

- 類別名稱不可空白。
- 類別名稱不可重複。
- 新增後立即更新 checkbox 清單。

### 9.3 新增自訂標籤

- 標籤名稱不可空白。
- 標籤名稱不可重複。
- 新增後立即更新篩選區與餐廳表單。

### 9.4 新增餐廳

- 餐廳名稱不可空白。
- 餐廳可選擇目標地點。
- 餐廳至少可選零個類別，但建議提示使用者加上類別或標籤比較好搜尋。
- 新增後立即出現在結果列表。

### 9.5 匯入清單

- 匯入前先顯示預覽，不直接寫入。
- 餐廳名稱相同且地址相同時，視為可能重複。
- 重複資料預設不新增。
- 匯入資料的 AI 建議標籤需標示為未確認。
- 使用者可在匯入後手動確認或修改標籤。

### 9.6 願望清單

- 點擊「下次吃」後切換餐廳的 `isWishlisted`。
- 願望清單篩選開啟時，只顯示已加入願望清單的餐廳。
- 黑名單餐廳即使在願望清單中，預設仍不顯示。

### 9.7 黑名單

- 點擊「避雷」後切換餐廳的 `isBlacklisted`。
- 被加入黑名單的餐廳預設從搜尋結果移除。
- 使用者開啟「顯示黑名單」時，才顯示黑名單餐廳。
- 黑名單餐廳不可被抽籤抽中。

### 9.8 搜尋結果

- 每次勾選或取消勾選條件後，結果即時更新。
- 無結果時顯示「目前沒有符合的餐廳」。
- 篩選狀態需自動保存，重新整理後仍保留。

### 9.9 抽籤

- 點擊抽籤後，從目前篩選結果中隨機選一間。
- 若目前沒有結果，不執行抽籤並顯示提示。
- 抽籤結果應清楚標示為目前推薦。

## 10. 錯誤與空狀態

需要處理：

- 尚未新增任何餐廳。
- 尚未新增任何目標地點。
- 尚未新增任何食物類別。
- 尚未新增任何自訂標籤。
- 搜尋無符合結果。
- 願望清單沒有任何餐廳。
- 黑名單篩選下沒有資料。
- 抽籤時沒有可抽餐廳。
- 匯入檔案格式錯誤。
- 匯入清單沒有餐廳。
- 匯入資料缺少餐廳名稱。
- 目標地點資料夾缺少必要文件。
- 輸入空白名稱。
- 重複新增同名類別或標籤。
- `localStorage` 讀取失敗或資料格式錯誤。

## 11. 第一版建議包含

第一版應包含：

- 新增目標地點。
- 新增食物類別。
- 新增自訂標籤。
- 新增餐廳。
- JSON 匯入清單。
- AI 建議標籤標示。
- 餐廳可勾選類別與標籤。
- 願望清單標記。
- 黑名單標記與預設排除。
- 條件篩選。
- 自動記住上一次篩選條件。
- 抽籤功能。
- 餐廳層級私人筆記。
- localStorage 保存。

第一版可暫緩：

- CSV 匯入。
- 多組常用條件儲存。
- 多筆造訪紀錄。
- 完整餐廳編輯。
- 類別與標籤刪除。
- 自動讀取資料夾。
- 實體地圖視覺化。

## 12. 第一版不包含

以下功能先不做，避免範圍變大：

- 後端 API
- 使用者登入
- 雲端同步
- 圖片上傳
- 地圖定位
- Google Maps API 串接
- 自動爬取 Google Maps
- 餐廳評分系統
- 複雜排序
- 多人共享

## 13. 第二版可擴充功能

未來可加入：

- 編輯餐廳
- 編輯或刪除類別
- 編輯或刪除自訂標籤
- CSV 匯入
- 匯出 JSON
- 多組常用篩選條件
- 多筆造訪紀錄
- 餐廳評分
- 價格區間
- 距離或地點
- 最近吃過紀錄
- 避免連續推薦同一間
- 以資料夾管理多個目標周圍美食地圖
- 地圖視覺化
- PWA 離線安裝

## 14. 驗收標準

第一版完成時應符合：

- 可以新增目標地點。
- 可以新增食物類別。
- 可以新增自訂標籤。
- 可以新增餐廳。
- 可以替餐廳選擇目標地點。
- 可以替餐廳選擇一個或多個食物類別。
- 可以替餐廳選擇一個或多個自訂標籤。
- 可以匯入線下 AI 整理後的 JSON 餐廳清單。
- 匯入資料可以保留 AI 建議標籤與使用者確認狀態。
- 可以將餐廳加入或移出願望清單。
- 可以將餐廳加入或移出黑名單。
- 黑名單餐廳預設不出現在搜尋結果。
- 可以依照目標地點篩選附近餐廳。
- 可以透過 checkbox 篩選餐廳。
- App 會記住上一次篩選條件。
- 可以從目前篩選結果中抽籤推薦一間。
- 抽籤不會抽到黑名單餐廳。
- 抽籤不會抽到目前目標地點以外的餐廳。
- 可以為餐廳留下私人筆記。
- 重新整理頁面後資料仍存在。
- 不需要任何後端或安裝流程。
